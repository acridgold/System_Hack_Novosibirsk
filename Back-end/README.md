# Flask Backend для SDEK Burnout AI

Полнофункциональный бэкенд на Python Flask для приложения диагностики синдрома выгорания сотрудников SDEK. Система предоставляет REST API для аутентификации, проведения диагностики выгорания, отслеживания метрик и управления рекомендациями по профилактике.

## 📋 Описание системы

**SDEK Burnout AI** - это AI-powered система диагностики синдрома выгорания и профилактики стресса для сотрудников логистической компании SDEK. Бэкенд обеспечивает:

- ✅ **Безопасную аутентификацию** через JWT токены
- ✅ **Диагностику выгорания** по шкале Maslach Burnout Inventory (MBI)
- ✅ **Отслеживание метрик** burnout, stress и productivity
- ✅ **Персонализированные рекомендации** по профилактике стресса
- ✅ **Историю диагностик** с анализом тенденций
- ✅ **Полное логирование** всех операций

## 🚀 Установка и запуск

### Требования
- Python 3.8+
- pip (пакетный менеджер Python)

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

Установятся следующие пакеты:
- `Flask` - основной веб-фреймворк
- `Flask-CORS` - поддержка кросс-доменных запросов
- `Flask-JWT-Extended` - управление JWT токенами
- `python-dotenv` - загрузка переменных окружения

### 2. Настройка переменных окружения

Файл `.env` уже создан с базовыми параметрами:

```env
FLASK_ENV=development
FLASK_APP=app.py
SECRET_KEY=your-secret-key-change-in-production
JWT_SECRET_KEY=your-jwt-secret-key-change-in-production
JWT_ACCESS_TOKEN_EXPIRES=86400
```

**Параметры:**
- `FLASK_ENV` - режим работы (development/production)
- `FLASK_APP` - точка входа приложения
- `SECRET_KEY` - секретный ключ Flask (изменить в production!)
- `JWT_SECRET_KEY` - ключ для подписи JWT токенов (изменить в production!)
- `JWT_ACCESS_TOKEN_EXPIRES` - время жизни токена в секундах (86400 = 24 часа)

### 3. Запуск сервера

```bash
python app.py
```

Сервер запустится на `http://localhost:8000` и будет готов к приему запросов.

## 📚 API Endpoints

Все защищенные endpoints требуют заголовок:
```
Authorization: Bearer {access_token}
```

### 🔐 Аутентификация (`/auth`)

#### POST `/auth/token` - Авторизация
Получить JWT токен для доступа к защищенным эндпоинтам.

**Формат запроса:** `application/x-www-form-urlencoded`
```
username: user@example.com
password: password123
```

**Успешный ответ (200):**
```json
{
  "access_token": "eyJ0eXAiOiJKV1QiLCJhbGc...",
  "token_type": "bearer",
  "user": {
    "id": 1,
    "email": "user@example.com",
    "name": "Иван Петров",
    "position": "Старший курьер",
    "department": "Отдел доставки"
  }
}
```

**Ошибки:**
- `400` - отсутствуют email или пароль
- `401` - неверный email или пароль

#### GET `/auth/verify` - Проверка токена
Проверить валидность JWT токена.

**Ответ (200):**
```json
{
  "valid": true,
  "user_id": 1,
  "user": {
    "id": 1,
    "email": "user@example.com",
    "name": "Иван Петров"
  }
}
```

**Ошибки:**
- `401` - токен невалиден или истек
- `404` - пользователь не найден

#### GET `/auth/me` - Данные текущего пользователя
Получить полную информацию о авторизованном пользователе.

**Ответ (200):**
```json
{
  "id": 1,
  "email": "user@example.com",
  "name": "Иван Петров",
  "position": "Старший курьер",
  "department": "Отдел доставки",
  "joinDate": "2023-01-15",
  "daysInSystem": 680,
  "completedRecommendations": 12,
  "avatar": null
}
```

### 📊 Диагностика (`/assessment`)

#### GET `/assessment/questions` - Список вопросов
Получить список всех 20 вопросов для диагностики выгорания (по шкале MBI).

**Ответ (200):**
```json
{
  "questions": [
    "Я чувствую себя эмоционально истощенным из-за работы",
    "Я чувствую усталость в конце рабочего дня",
    ...
  ],
  "total": 20
}
```

#### POST `/assessment/submit` - Отправка ответов
Отправить ответы на диагностику и получить результат выгорания.

**Body (JSON):**
```json
{
  "answers": {
    "0": 3,
    "1": 4,
    "2": 3,
    ...
    "19": 3
  }
}
```

**Ответ (201):**
```json
{
  "id": 1,
  "burnoutLevel": "medium",
  "score": 62,
  "emotionalExhaustion": 28,
  "depersonalization": 16,
  "reducedAccomplishment": 18,
  "timestamp": "2025-11-11T10:30:00Z"
}
```

**Показатели:**
- `burnoutLevel` - уровень выгорания (low/medium/high)
- `score` - общий балл (0-120)
- `emotionalExhaustion` - эмоциональное истощение (0-54)
- `depersonalization` - деперсонализация (0-30)
- `reducedAccomplishment` - снижение личных достижений (0-36)

**Ошибки:**
- `400` - неполные или отсутствующие ответы
- `401` - токен невалиден

#### GET `/assessment/history` - История диагностик
Получить полную историю диагностик текущего пользователя (в обратном хронологическом порядке).

**Ответ (200):**
```json
{
  "assessments": [
    {
      "id": 1,
      "userId": 1,
      "date": "2025-11-11",
      "timestamp": "2025-11-11T10:30:00Z",
      "burnoutLevel": "medium",
      "score": 62,
      "emotionalExhaustion": 28,
      "depersonalization": 16,
      "reducedAccomplishment": 18,
      "answers": {...}
    },
    ...
  ],
  "total": 5
}
```

#### GET `/assessment/{id}` - Детали диагностики
Получить полную информацию о конкретной диагностике со всеми ответами.

**Параметры пути:**
- `id` - ID диагностики

**Ответ (200):**
```json
{
  "id": 1,
  "userId": 1,
  "date": "2025-11-11",
  "burnoutLevel": "medium",
  "score": 62,
  "answers": {...}
}
```

**Ошибки:**
- `404` - диагностика не найдена

### 📈 Дашборд (`/dashboard`)

#### GET `/dashboard/metrics` - Метрики
Получить метрики за последние 7 дней (burnout, stress, productivity).

**Ответ (200):**
```json
{
  "metrics": [
    {"date": "Пн", "burnout": 82, "stress": 85, "productivity": 18},
    {"date": "Вт", "burnout": 78, "stress": 80, "productivity": 22},
    ...
  ],
  "period": "week",
  "total": 7
}
```

#### GET `/dashboard/summary` - Сводка дашборда
Получить сводку дашборда: последнюю диагностику и метрики.

**Ответ (200):**
```json
{
  "latestAssessment": {
    "date": "2025-11-11",
    "burnoutLevel": "medium",
    "score": 62,
    "emotionalExhaustion": 28,
    "depersonalization": 16,
    "reducedAccomplishment": 18
  },
  "metrics": [...],
  "totalAssessments": 5
}
```

### 💡 Рекомендации (`/recommendations`)

#### GET `/recommendations` - Список рекомендаций
Получить все рекомендации для текущего пользователя с их статусом выполнения.

**Ответ (200):**
```json
{
  "recommendations": [
    {
      "id": 1,
      "userId": 1,
      "category": "Медитация",
      "title": "Практикуйте осознанность",
      "description": "Уделяйте 10 минут в день медитации или дыхательным упражнениям для снижения стресса",
      "priority": "high",
      "duration": "10 мин/день",
      "completed": true
    },
    ...
  ],
  "total": 6
}
```

#### GET `/recommendations/{id}` - Детали рекомендации
Получить полную информацию о конкретной рекомендации.

**Параметры пути:**
- `id` - ID рекомендации

**Ответ (200):**
```json
{
  "id": 1,
  "category": "Медитация",
  "title": "Практикуйте осознанность",
  "description": "...",
  "priority": "high",
  "duration": "10 мин/день",
  "completed": true
}
```

#### POST `/recommendations/{id}/complete` - Отметить выполненной
Отметить рекомендацию как выполненную пользователем.

**Ответ (200):**
```json
{
  "id": 1,
  "completed": true,
  "message": "Recommendation marked as completed"
}
```

#### POST `/recommendations/{id}/incomplete` - Отметить невыполненной
Отметить рекомендацию как невыполненную.

**Ответ (200):**
```json
{
  "id": 1,
  "completed": false,
  "message": "Recommendation marked as incomplete"
}
```

## 🧪 Тестовые учетные данные

Для тестирования используйте следующие данные:

| Параметр | Значение |
|----------|----------|
| **Email** | `user@example.com` |
| **Password** | `password123` |

## 📝 Логирование

Система полностью логирует все операции для отладки и мониторинга.

### Структура логов

Логи сохраняются в папке `logs/` с автоматической ротацией (максимум 10MB на файл):

- **app.log** - логи главного приложения (инициализация, ошибки)
- **auth.log** - логи аутентификации (попытки входа, проверка токенов)
- **assessment.log** - логи диагностики (получение/отправка тестов, результаты)
- **dashboard.log** - логи дашборда (запросы метрик)
- **recommendations.log** - логи рекомендаций (получение, изменение статуса)

### Уровни логирования

- 🟢 **INFO** - нормальные операции (успешный вход, получение данных)
- 🟡 **WARNING** - предупреждения (неверные учетные данные, ресурсы не найдены)
- 🔴 **ERROR** - ошибки с полной трассировкой стека (ошибки обработки, исключения)

### Пример логов

```
2025-11-11 10:30:15,123 - auth - INFO - [login:30] - Попытка входа для пользователя: user@example.com
2025-11-11 10:30:15,456 - auth - INFO - [login:50] - Пользователь user@example.com (ID: 1) успешно авторизован
2025-11-11 10:31:00,789 - assessment - INFO - [submit_assessment:85] - Получена диагностика от пользователя ID: 1
2025-11-11 10:31:01,234 - assessment - INFO - [submit_assessment:110] - Диагностика обработана для пользователя ID: 1. Уровень выгорания: medium, Балл: 62
```

## 🏗️ Архитектура проекта

### Структура файлов

```
Back-end/
├── app.py                  # Главное приложение Flask (точка входа)
├── models.py               # Модели данных и in-memory БД
├── logger.py               # Конфигурация логирования
├── requirements.txt        # Зависимости Python
├── .env                    # Переменные окружения
├── .gitignore              # Git исключения
├── logs/                   # Папка с логами (создается автоматически)
│   ├── app.log
│   ├── auth.log
│   ├── assessment.log
│   ├── dashboard.log
│   └── recommendations.log
├── routes/                 # Blueprints с маршрутами API
│   ├── __init__.py
│   ├── auth.py            # Endpoints: /auth/token, /auth/verify, /auth/me
│   ├── assessment.py      # Endpoints: /assessment/questions, /assessment/submit, /assessment/history
│   ├── dashboard.py       # Endpoints: /dashboard/metrics, /dashboard/summary
│   └── recommendations.py # Endpoints: /recommendations/*
└── README.md              # Документация проекта
```

### Компоненты системы

#### 1. **app.py** - Главное приложение
- Инициализация Flask приложения
- Конфигурация CORS и JWT
- Регистрация blueprints
- Обработка ошибок (401, 404, 500)
- Логирование операций приложения

#### 2. **models.py** - Модели данных
- `USERS_DB` - хранилище пользователей
- `ASSESSMENTS_DB` - история диагностик
- `METRICS_DB` - метрики дашборда
- `RECOMMENDATIONS_DB` - рекомендации
- `ASSESSMENT_QUESTIONS` - 20 вопросов по шкале MBI

#### 3. **logger.py** - Система логирования
- Централизованная конфигурация логирования
- Ротация логов по размеру (10MB)
- Отдельные логгеры для каждого компонента
- Форматирование с временем, уровнем и деталями

#### 4. **routes/** - API маршруты

**auth.py:**
- JWT авторизация
- Проверка токена
- Получение данных пользователя

**assessment.py:**
- Вопросы для диагностики (MBI шкала)
- Обработка ответов и расчет показателей
- История диагностик
- Логирование результатов

**dashboard.py:**
- Метрики за 7 дней
- Сводка с последней диагностикой
- Анализ тенденций

**recommendations.py:**
- Список рекомендаций
- Отметка выполнения/невыполнения
- Детали рекомендации

## 🔒 Безопасность

### Аутентификация

- **JWT токены** - все защищенные endpoints требуют валидный JWT токен
- **Время жизни токена** - 24 часа (настраивается в `.env`)
- **Автоматический refresh** - фронтенд перенаправляет на логин при 401 ошибке

### Защита данных

- **CORS** - включен для всех источников (в production ограничить!)
- **Валидация входных данных** - проверка полноты и формата данных
- **Безопасная передача** - используется HTTPS в production

### Рекомендации для production

```env
# Измените эти значения в production:
SECRET_KEY=generate-secure-random-string
JWT_SECRET_KEY=generate-secure-random-string
FLASK_ENV=production

# Ограничьте CORS:
CORS(app, resources={
    r"/*": {
        "origins": ["https://yourdomain.com"],
        "methods": ["GET", "POST", "PUT", "DELETE"],
        "allow_headers": ["Content-Type", "Authorization"]
    }
})

# Используйте HTTPS
# Установите Secure и HttpOnly флаги для cookies
# Добавьте SSL сертификаты
```

## 💾 Хранилище данных

В текущей реализации используется **in-memory хранилище** (Python dictionaries):

```python
USERS_DB = {
    'user@example.com': { ... }
}

ASSESSMENTS_DB = {
    1: [ { ... }, { ... } ]  # История диагностик пользователя
}
```

**Характеристики:**
- ✅ Быстрый доступ
- ✅ Простая реализация для прототипа
- ❌ Данные теряются при перезагрузке
- ❌ Не масштабируется для production

**Для production рекомендуется:**
- PostgreSQL или MySQL
- MongoDB для гибкой схемы
- Redis для кэширования сессий

## 📊 Алгоритм расчета выгорания

Используется шкала **Maslach Burnout Inventory (MBI)**:

### Категории вопросов:

1. **Эмоциональное истощение (EE)** - вопросы 0-8
   - Диапазон: 0-54 балла
   - Высокое = > 27

2. **Деперсонализация (DP)** - вопросы 9-13
   - Диапазон: 0-30 баллов
   - Высокое = > 10

3. **Снижение личных достижений (PA)** - вопросы 14-19
   - Диапазон: 0-36 баллов
   - Низкое (высокий уровень выгорания) = < 17

### Определение уровня выгорания:

- **LOW** (низкий) - общий балл < 50
- **MEDIUM** (средний) - общий балл 50-69
- **HIGH** (высокий) - общий балл ≥ 70

## 🛠️ Технологический стек

| Компонент | Версия | Назначение |
|-----------|--------|-----------|
| Python | 3.8+ | Язык программирования |
| Flask | 2.3.3 | Веб-фреймворк |
| Flask-CORS | 4.0.0 | CORS middleware |
| Flask-JWT-Extended | 4.5.2 | JWT аутентификация |
| python-dotenv | 1.0.0 | Управление ENV переменными |
| Werkzeug | 2.3.7 | WSGI утилиты |

## 🚀 Развертывание

### Локальная разработка

```bash
python app.py
```

### Production с Gunicorn

```bash
pip install gunicorn
gunicorn -w 4 -b 0.0.0.0:8000 app:app
```

### Docker (опционально)

```dockerfile
FROM python:3.9-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
CMD ["gunicorn", "-w", "4", "-b", "0.0.0.0:8000", "app:app"]
```

## 📞 Контакты и поддержка

- **Проект:** SDEK Burnout AI - System Hack Novosибирск
- **Бэкенд:** Python Flask REST API
- **Фронтенд:** React + Redux + Material-UI
- **Тестовые данные:** встроены в `models.py`

## 📄 Лицензия

Проект разработан для System Hack Novosибирск 2025.
